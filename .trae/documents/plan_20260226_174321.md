# Python Wiki 持续架构优化计划

## 技术选型升级

| 模块 | 当前方案 | 建议方案 | 理由 |
|------|---------|---------|------|
| 向量存储 | FAISS | FAISS + BM25 | 混合检索效果更好 ✅已实现 |
| 搜索引擎 | 无 | Whoosh | 本地全文搜索 |
| Agent 框架 | 自研 | LangGraph | 成熟的工作流编排 |
| 配置管理 | Pydantic | Pydantic V2 | 性能提升 |
| 异步框架 | asyncio | asyncio + anyio | 更好的异步支持 |

---

## Phase 5: 技术选型升级（高优先级）

### 5.1 依赖更新
```toml
# 新增依赖
langgraph = "^0.0.40"
whoosh = "^2.7.4"
anyio = "^4.0.0"

# 升级依赖
pydantic = "^2.5.0"  # 已是 V2
pydantic-settings = "^2.1.0"  # 已是 V2
```

### 5.2 LangGraph 集成
**文件**: `agents/langgraph/`
- `__init__.py` - 模块初始化
- `graph_builder.py` - 图构建器
- `state.py` - 状态定义（TypedDict）
- `nodes.py` - 节点定义
- `edges.py` - 边和条件路由
- `checkpointer.py` - 状态持久化

### 5.3 Whoosh 全文搜索
**文件**: `search/whoosh_index.py`
- 创建 Whoosh 索引
- 支持中文分词
- 与 FAISS 混合检索

### 5.4 Anyio 异步支持
**文件**: 更新现有异步代码
- 使用 anyio 替代纯 asyncio
- 支持多后端（asyncio/trio）

---

## Phase 6: Git 原生集成（高优先级）

### 6.1 文件清单
- `sync/git_change_detector.py` - Git diff 变更检测
- `sync/doc_lag_detector.py` - 文档滞后检测
- `sync/git_hooks.py` - Git 钩子集成

---

## Phase 7: 文档质量评估（高优先级）

### 7.1 文件清单
- `wiki/quality_scorer.py` - 文档质量评分
- `wiki/improvement_suggester.py` - 改进建议

---

## Phase 8: 多模型路由（中优先级）

### 8.1 文件清单
- `llm/model_router.py` - 智能模型选择
- `llm/model_capability.py` - 模型能力评估

---

## 新增文件清单

```
src/pywiki/agents/langgraph/
├── __init__.py
├── graph_builder.py      # LangGraph 图构建器
├── state.py              # 状态定义
├── nodes.py              # 节点定义
├── edges.py              # 边和条件路由
└── checkpointer.py       # 状态持久化

src/pywiki/search/
└── whoosh_index.py       # Whoosh 全文索引

src/pywiki/sync/
├── git_change_detector.py    # Git 变更检测
├── doc_lag_detector.py       # 文档滞后检测
└── git_hooks.py              # Git 钩子

src/pywiki/wiki/
├── quality_scorer.py         # 文档质量评分
└── improvement_suggester.py  # 改进建议

src/pywiki/llm/
├── model_router.py           # 模型路由
└── model_capability.py       # 模型能力
```

---

## LangGraph 架构设计

```python
# state.py
from typing import TypedDict, Annotated
from langgraph.graph.message import add_messages

class WikiState(TypedDict):
    messages: Annotated[list, add_messages]
    documents: list[dict]
    context: dict
    current_node: str
    errors: list[str]

# graph_builder.py
from langgraph.graph import StateGraph

def build_wiki_graph():
    graph = StateGraph(WikiState)
    graph.add_node("parse", parse_node)
    graph.add_node("analyze", analyze_node)
    graph.add_node("generate", generate_node)
    graph.add_edge("parse", "analyze")
    graph.add_edge("analyze", "generate")
    return graph.compile()
```

---

## 验收标准

### Phase 5
- [ ] LangGraph 工作流正常运行
- [ ] Whoosh 全文搜索可用
- [ ] Anyio 异步兼容

### Phase 6
- [ ] Git diff 变更检测
- [ ] 文档滞后检测

### Phase 7
- [ ] 文档质量评分 > 85% 准确率

### Phase 8
- [ ] 多模型智能路由