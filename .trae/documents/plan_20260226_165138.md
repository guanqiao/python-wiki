
# Python Wiki 完整修复与完善计划

## 📊 当前状态分析

### 发现的问题
1. **两套记忆系统冲突**：
   - 之前创建的：`personal.py`, `project.py`, `solutions.py`
   - 新创建的：`memory_entry.py`, `global_memory.py`, `project_memory.py`, `memory_manager.py`, `style_learner.py`

2. **语法错误**：
   - `project_memory.py` 第 38 行：JSON 格式化错误
   - `style_learner.py` 第 189 行和第 276 行：语法错误

3. **需要统一**：保留更完整的新记忆系统，删除旧的重复文件

---

## 🎯 实施计划

### Phase 1: 修复语法错误（优先级：最高）
1. 修复 `project_memory.py` 的语法错误
2. 修复 `style_learner.py` 的语法错误
3. 修复 `memory_entry.py` 的 `to_dict()` 方法中的语法问题

### Phase 2: 统一记忆系统（优先级：高）
1. 保留新的完整记忆系统：
   - `memory_entry.py` - 记忆条目定义
   - `global_memory.py` - 全局记忆管理
   - `project_memory.py` - 项目记忆管理
   - `memory_manager.py` - 统一记忆管理器
   - `style_learner.py` - 编码风格学习器
2. 删除旧的重复文件：
   - `personal.py`
   - `project.py`
   - `solutions.py`
3. 更新 `memory/__init__.py` 导出新模块

### Phase 3: 完善核心功能（优先级：高）
1. 确保导出功能 `wiki/export.py` 正常工作
2. 确保问答面板 `gui/panels/qa_panel.py` 正常工作
3. 确保主窗口 `gui/main_window.py` 正确集成问答面板
4. 确保 Agent 集成 `agent/` 模块正常工作

### Phase 4: 验证与测试（优先级：中）
1. 运行静态类型检查（mypy）
2. 运行代码格式化（black）
3. 运行代码 lint（ruff）
4. 验证所有模块可以正常导入

---

## 📁 文件变更清单

### 需要修复的文件
- `src/pywiki/memory/memory_entry.py`
- `src/pywiki/memory/project_memory.py`
- `src/pywiki/memory/style_learner.py`

### 需要删除的文件
- `src/pywiki/memory/personal.py`
- `src/pywiki/memory/project.py`
- `src/pywiki/memory/solutions.py`

### 需要更新的文件
- `src/pywiki/memory/__init__.py`
