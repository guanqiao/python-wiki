## 测试计划

### 现状分析
- `improvement_suggester.py` 已有完整测试覆盖
- 核心模块缺少测试：
  - `quality_scorer.py` - 文档质量评分器
  - `storage.py` - Wiki存储服务
  - `manager.py` - Wiki管理器

### 测试添加计划

#### 1. 创建 `tests/wiki/test_quality_scorer.py`
- 测试 `QualityScore` 数据类
- 测试 `DocQualityReport` 数据类
- 测试 `QualityScorer` 核心方法：
  - `score_document()` - 单文档评分
  - `score_all_documents()` - 全部文档评分
  - `_score_completeness()` - 完整性评分
  - `_score_readability()` - 可读性评分
  - `_score_timeliness()` - 时效性评分
  - `_score_structure()` - 结构评分
  - `_score_examples()` - 示例评分
  - `_find_issues()` - 问题发现
  - `get_quality_summary()` - 质量摘要

#### 2. 创建 `tests/wiki/test_storage.py`
- 测试 `WikiStorage` 初始化
- 测试 `save_document()` - 保存文档
- 测试 `get_document()` - 获取文档
- 测试 `delete_document()` - 删除文档
- 测试 `list_documents()` - 列出文档
- 测试 `search()` - 搜索功能
- 测试历史版本保存

#### 3. 创建 `tests/wiki/test_manager.py`
- 测试 `WikiManager` 初始化
- 测试 `GenerationProgress` 状态管理
- 测试 `generate_full()` 完整生成流程
- 测试 `generate_incremental()` 增量更新
- 测试进度回调机制

#### 4. 运行测试验证
- 执行 `pytest tests/` 运行全部测试
- 确保所有测试通过