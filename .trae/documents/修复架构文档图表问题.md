## 问题总结

### 1. 图表显示问题
- C4 图表（系统上下文、容器、组件、依赖关系、数据流图）**缺少 `mermaid` 代码块标记**，无法渲染

### 2. 抽象度不够
- 子系统视图过于简单，只显示3个节点，未反映真实业务模块
- 分层视图将具体类（如 PageResult、BaseMapperX）作为节点，层次太低
- 缺少业务领域划分（系统管理、商城、工作流等）

### 3. 数据不准确
- 包名解析错误：`oder` 是截断的 `me` 包名，`opensource` 是 Python 脚本路径混入
- 架构风格矛盾：说是分层架构，但图表展示类级关系
- 分层归类错误：枚举、接口被错误归到数据层
- 指标数据不一致：函数数显示为2，但热点模块有方法数

## 修复方案

1. **修复 Mermaid 渲染** - 为所有 C4 图表添加 ```mermaid 标记
2. **重新设计子系统视图** - 按业务模块划分（system/trade/bpm/ai 等）
3. **修正分层视图抽象级别** - 从类级别提升到组件/模块级别
4. **修复 C4 容器图** - 正确识别 Web应用、Admin应用、数据库等容器
5. **清理错误数据** - 移除 Python 残留，修正包名解析逻辑
6. **重新归类分层** - 枚举、工具类归到基础设施层

请确认后我将执行修复。