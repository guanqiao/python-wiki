## 问题分析

`ProjectAnalyzeThread` 的 `run` 方法只调用了 `TechStackAnalyzer`，其他分析器完全没有被使用，导致：
- 设计模式标签页永远为空（patterns 参数始终为 `[]`）
- 架构演进标签页永远为空（`update_evolution` 从未被调用）
- 业务逻辑标签页永远为空（`update_business_logic` 从未被调用）

## 修复方案

### 1. 修改 `ProjectAnalyzeThread` 类 ([main_window.py:256-283](d:\opensource\github\python-wiki\src\pywiki\gui\main_window.py#L256-L283))

- 扩展 `analysis_completed` 信号，增加 `evolution` 和 `business_logic` 参数
- 在 `run` 方法中添加：
  - 设计模式检测（使用 `DesignPatternDetector`）
  - 业务逻辑分析（使用 `BusinessLogicAnalyzer`）
  - 架构演进分析（使用 `ArchitectureEvolutionTracker`）

### 2. 修改 `_on_analyze_completed` 方法 ([main_window.py:941-951](d:\opensource\github\python-wiki\src\pywiki\gui\main_window.py#L941-L951))

- 更新方法签名接收新参数
- 调用 `update_evolution()` 更新架构演进标签页
- 调用 `update_business_logic()` 更新业务逻辑标签页

### 3. 改进错误处理

- 捕获并显示具体错误信息到进度面板，而不是静默吞掉异常